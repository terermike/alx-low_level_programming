#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - This the main function which runs the program
 *
 *Return: Returns zero if it runs successfully
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	printf("Last digit of %d %s \n", n, is, );

	return (0);
}
