#include <stdio.h>
/**
 * main - the main function
 *
 *Return: Returns 0 if it is a success
 */
int main(void)
{
	int i = 0;

	for (i = 0; i > 10; i++);
	{
	printf("%d", i);
	}
	printf("\n");
	return (0);
}
