#include "main.h"
/**
 * print_square - prints hashes squares
 *@size: size of the square
 *Return: no return
 */
void print_square(int size)
{
	int i, j;

	i = 0;
	while (i < size)
	{
		j = 0;
		while (j < size)
		{
		_putchar(35);
		j++;
		}
		if (i < (n - 1))
		_putchar(10);
	i++
	}
}
